#include <stdio.h>

int main(void)
{
    int width = 5, length = 4, area = width * length;

    printf("Dimensions: %dx%d\n", width, length);
    printf("Area of rectangl: %d\n", area);

    /*
    printf("Area of rectangl: %d\n",width * length);
    */

    return 0;
}